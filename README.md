# Strapi docker integration

This contains both the local and production environment settings for strapi, mysql via docker-compose.

## Todo

Create `.env` file with the example file and fill with secure values.
